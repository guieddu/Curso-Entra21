// 1) if (a string with zero)
    // Yes





// 2) The name of JavaScript
    // const jsName = prompt('What is the "official" name of JavaScript?');
    // if (jsName === "ECMAScript") {
    //     console.log("Right");
    // } else {
    //     console.log(`You don't know? "ECMAScript"!`);
    // }




// 3) Show the sign
    // let result;
    // result = (a + b < 4) ? 'Below' : 'Over';



// 4) Rewrite 'if..else' into '?'
    // let message;
    // message = (login === "Employee") ? "Hello" : (login === "Director") ? "Greetings" : (login === "") ? "No login" : "";